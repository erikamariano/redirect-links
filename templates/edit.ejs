<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

    <title>Edit Link</title>
</head>
<body>
    <div class="container w-50">
        <h1>Edit link:</h1>

        <form class="d-flex flex-column" action="/edit/<%=body.id%>" method="POST">
            <input class="mb-2" name="title" placeholder="Title" required value="<%=body.title%>">
            <input class="mb-2" name="description" placeholder="Description" value="<%=body.description%>">
            <input class="mb-2" name="url" placeholder="URL" required value="<%=body.url%>">
            <button class="btn btn-primary">Update</button>
        </form>

        <%if(error){%>
            <div class="alert alert-danger mt-3">
            <!-- Trata o erro vindo do backend -->
                    <strong>Error: </strong><h3><%=error.message%><h3>            
            </div>
        <%}%>
    </div>
</body>
</html>